import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['nt1admin.consociate.co.in'], // Add your domain here
  },
};


export default nextConfig;
